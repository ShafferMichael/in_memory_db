# in_memory_db

## How to Run

This project uses Python 3 & no other libraries

Instructions to run code:

1. Download or clone the repo
2. Navigate to the project directory in terminal
3. Run: `python3 in_mem_db.py`

## Suggested Improvements

To make it a formal assignment it would help to clarify if multiple puts on the same key during a transaction are allowed or just expand on the logic of the database to make it more specific. Also using predefined unit tests instead of print statements would make grading more objective and realistic and easier to understand. I think it would be helpful to provide a sample expected output file or test cases. A more robust test set could be nice as well. Sample expected outputs would also make debugging easier.
